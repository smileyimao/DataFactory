# 🗺️ DataFactory 数字化工厂 - MLOps 演进路线图 (v2026.02)

## 🏗️ 阶段一：标准化与精细化生产 (v1.x) - [当前冲刺阶段]
*核心：建立严谨的工业级准入机制与人机交互闭环*

- [x] **环境变量管理**：敏感信息（邮件密码、路径、API Key）实现 `.env` 隔离。
- [x] **批处理复核流水线**：实现异步扫描与集中审批逻辑。
- [ ] **多伦多时区本地化**：全系统（日志、邮件、数据库）统一使用 `America/Toronto` 时间戳。
- [ ] **工业级 Logging 系统**：记录 `[时间][级别][模块]` 格式日志，涵盖指纹、DB命中、YOLO得分及决策指令。
- [ ] **物理隔离归档对齐**：废品库（rejected）与冗余库（redundant）强制实现 Batch 文件夹对齐，确保物理层面的可追溯性。

## 🧠 阶段二：视觉感知与自动化准入 (v2.x)
*核心：引入深度学习模型与实验管理 (Experiment Tracking)*

- [ ] **YOLO 视觉质检接入**：
    - **单例加载**：全局唯一模型实例。
    - **智能抽检**：实现等间隔采样推理，平衡 FPS 与精度。
- [ ] **版本映射 (Version Mapping)**：**[顶级工业标准]** - 在日志与元数据中记录当前生效的算法版本（如 `YOLOv8-v1.2`）。
    - 确保每一条数据都能溯源到其对应的检测逻辑版本。
- [ ] **“双门槛”自适应准入**：自动放行（高分）、自动拦截（极低分）、人工审核（中间态）。
- [ ] **MLflow Tracking**：记录实验参数（门槛、模型版本）与批次质量指标分布。

## 🐳 阶段三：高并发与云原生架构 (v3.x)
*核心：解决海量数据吞吐下的系统扩展性*

- [ ] **任务状态机 (State Machine)**：数据库引入 `Pending/Processing/Reviewing/Done/Fail` 状态。
- [ ] **多进程/分布式 Worker**：利用消息队列实现多视频并行质检，消除单机 I/O 瓶颈。
- [ ] **容器化部署 (Docker)**：实现环境一键迁移，结合 Prometheus 监控系统资源占用。

## 🚀 阶段四：真实生产环境适配与深度溯源 (v4.x)
*核心：数据血缘 (Data Lineage) 与系统性能总结*

- [ ] **转换算子记录 (Transform Log)**：**[顶级工业标准]** - 详细记录数据加工过程中的每一个算子参数（如：抽帧率、压缩码率、分辨率、色彩空间等）。
- [ ] **可视化血缘图谱 (Data Lineage Graph)**：**[顶级工业标准]** - 搭建 UI 界面，实现点击 `Batch_ID` 即可直观查看 100% 数据流转图谱