# 建议 Python 3.12；安装: pip install -c constraints.txt -r requirements.txt
numpy>=1.26,<2
# DataFactory v1.6 — 环境依赖（Python 3.9+）
PyYAML>=6.0
opencv-python-headless>=4.5,<4.10
tqdm>=4.60
pandas>=1.3,<3
matplotlib>=3.5
python-dotenv>=1.0
watchdog>=2.0
inputimeout>=1.0.4

# ---------- v2.0 新增依赖 ----------
# 视觉质检：YOLO 单例加载、智能抽检（engines/vision_detector.py）
ultralytics>=8.0
# MLflow：批次级实验与指标（config mlflow.enabled 开启后生效）
mlflow>=2.0
