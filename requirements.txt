# DataFactory 运行依赖（Python 3.9+）
# 安装: pip install -r requirements.txt
#
# 若 ultralytics 报错（如 ResolutionImpossible、依赖 torch 等）：
# 1) 优先用当前环境，上午能跑就不要再重装——本文件仅作依赖清单，不必反复 pip install -r。
# 2) 必须重装时：先升级 pip（pip install -U pip），再新建虚拟环境后 pip install -r requirements.txt。
# 3) 或先装 torch 再装其余：pip install torch  &&  pip install -r requirements.txt

# 配置与环境
python-dotenv>=1.0.0
PyYAML>=6.0

# 视频/图像与质检
opencv-python>=4.8.0
numpy>=1.24.0

# 视觉模型（YOLO 8.x，禁止解析到 0.0.x 旧版）
ultralytics>=8.0.0,<9.0.0

# 报告与图表
pandas>=2.0.0
matplotlib>=3.7.0

# 进度条与交互
tqdm>=4.65.0
inputimeout>=1.0.0

# Guard 模式（监控 raw 目录）
watchdog>=3.0.0

# 可选：MLflow 批次实验记录（config mlflow.enabled 为 true 时使用）
mlflow>=2.0.0

# 厂长中控台（review.mode=dashboard 时使用）
fastapi>=0.100.0
uvicorn>=0.22.0
