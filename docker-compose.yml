services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: datafactory
      POSTGRES_USER: datafactory
      POSTGRES_PASSWORD: datafactory
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
