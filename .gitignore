# 项目虚拟环境（只用 venv，不用 conda）
.venv/

# 排除所有数据产出（旧根目录名 + 新 storage/db 下的数据）
data_warehouse/
raw_video/
test_videos/
rejected_material/
storage/archive/
storage/raw/
storage/rejected/
storage/test/
storage/reports/
storage/for_labeling/
db/*.db
db/*.sqlite
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
.DS_Store

# 本地私密环境变量文件（只在自己电脑上用，不提交）
.env

# 日志目录（按日滚动，不提交）
logs/

# Obsidian 知识库（个人笔记，不提交）
Knowledge/

# 测试产出（工业报表预览等，本地查看用）
tests/output/

# MLflow 与模型文件（本地运行生成，不提交）
mlruns/
runs/
mlflow.db
mlflow_artifacts/
models/*.pt
models/registry_cache/
storage/training/
storage/labeled_return/INTERVIEW_PREP.md
